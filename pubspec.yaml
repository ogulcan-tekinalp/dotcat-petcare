name: dotcat
description: "dotcat PetCare - Cat Care Assistant"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  sqflite: ^2.4.2
  path: ^1.9.1
  path_provider: ^2.1.2
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.0
  uuid: ^4.5.1
  intl: ^0.20.2
  image_picker: ^1.0.7
  image_cropper: ^8.0.2
  shared_preferences: ^2.2.2
  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.1
  firebase_storage: ^12.3.4
  google_sign_in: ^6.2.2
  # Calendar
  table_calendar: ^3.1.2
  # UI & Design
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  lottie: ^3.0.0
  confetti: ^0.7.0
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
